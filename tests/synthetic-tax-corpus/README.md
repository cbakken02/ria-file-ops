# Synthetic Tax Corpus

This corpus uses synthetic, non-client tax document text fixtures to evaluate tax document classification, primary metadata, and v2 line/box fact extraction.

The fixtures represent extracted PDF/OCR text rather than rendered PDFs. That keeps this corpus focused on tax taxonomy and field extraction behavior; PDF rendering/extraction is covered by the statement and identity corpora.

